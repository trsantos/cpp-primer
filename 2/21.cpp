// int i = 0;

// double* dp = &i;
// Invalid because we can't assign an address of an int to a pointer to double.

// int *ip = i;
// Valid. Same as int *ip = 0;

// int *p = &i;
// Also valid, but now p points to the address of the object i.
